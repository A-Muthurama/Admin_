[build]
rootDirectory = "Backend"
buildCommand = "npm install && npx prisma generate && npx prisma migrate deploy && npx nest build"

[deploy]
startCommand = "node dist/src/main.js"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
